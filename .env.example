# Server-only. Never put this key in frontend code or commit a real .env file.
MISTRAL_API_KEY=your_mistral_api_key_here

# Optional; mistral-small-latest is used when this is empty.
MISTRAL_MODEL=mistral-small-latest

# Local relay defaults.
PORT=8080
DB_PATH=relay.db